import { defineConfig } from "vite";
// Import plugins that are needed for your project

export default defineConfig({
  // Example: If you use React
  plugins: [
    // Add React plugin here for JS
  ],
  server: {
    port: 3000,
  },
});
